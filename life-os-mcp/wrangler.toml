name = "life-os-mcp"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Non-secret vars go here. Secrets are set via: npx wrangler secret put <KEY>
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, MCP_API_KEY are all secrets.
